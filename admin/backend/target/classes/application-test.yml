# 测试环境配置
# 激活方式：--spring.profiles.active=test

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 测试服务器 MySQL
    url: jdbc:mysql://192.168.1.100:3306/backend?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&connectTimeout=30000
    username: test_user
    password: test123456
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20

  data:
    redis:
      host: 192.168.1.100
      port: 6379
      password: redis123

# JWT 配置
jwt:
  validity: 604800000         # 7天
  renewal-threshold: 3600000   # 1小时

# 日志级别
logging:
  level:
    com.example: DEBUG
